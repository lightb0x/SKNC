import axios from 'axios';

import {
  UPLOAD_DOCX,
  FETCH_IMG,
  FETCH_HTML,
} from './types';

export const uploadDocx = () => (dispatch) => {

};

export const fetchImages = () => (dispatch) => {

};

export const fetchHTML = () => (dispatch) => {

};